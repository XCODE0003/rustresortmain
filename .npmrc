## Always install optional deps (workaround npm bug #4828 with platform-specific
## native bindings like @tailwindcss/oxide-linux-x64-gnu, @rollup/rollup-*).
## Without this the lock file generated on macOS/Windows can prevent the linux
## binding from being installed on the deploy server.
include=optional
