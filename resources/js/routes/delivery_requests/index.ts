import pricecap from './pricecap'
import status from './status'

const delivery_requests = {
    pricecap: Object.assign(pricecap, pricecap),
    status: Object.assign(status, status),
}

export default delivery_requests