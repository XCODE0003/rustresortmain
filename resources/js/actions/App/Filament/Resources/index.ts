import Articles from './Articles'
import Banners from './Banners'
import CaseopenHistories from './CaseopenHistories'
import Cases from './Cases'
import CasesItems from './CasesItems'
import DeliveryRequests from './DeliveryRequests'
import Donates from './Donates'
import Faqs from './Faqs'
import GuideCategories from './GuideCategories'
import Guides from './Guides'
import Inventories from './Inventories'
import LegalPages from './LegalPages'
import PaymentGateways from './PaymentGateways'
import PromoCodes from './PromoCodes'
import RconTasks from './RconTasks'
import ServerCategories from './ServerCategories'
import Servers from './Servers'
import ShopCategories from './ShopCategories'
import ShopCoupons from './ShopCoupons'
import ShopItems from './ShopItems'
import ShopPurchases from './ShopPurchases'
import ShopSets from './ShopSets'
import ShopStatistics from './ShopStatistics'
import SocialLinks from './SocialLinks'
import Streams from './Streams'
import Tickets from './Tickets'
import Users from './Users'
import Visits from './Visits'
import WonItems from './WonItems'

const Resources = {
    Articles: Object.assign(Articles, Articles),
    Banners: Object.assign(Banners, Banners),
    CaseopenHistories: Object.assign(CaseopenHistories, CaseopenHistories),
    Cases: Object.assign(Cases, Cases),
    CasesItems: Object.assign(CasesItems, CasesItems),
    DeliveryRequests: Object.assign(DeliveryRequests, DeliveryRequests),
    Donates: Object.assign(Donates, Donates),
    Faqs: Object.assign(Faqs, Faqs),
    GuideCategories: Object.assign(GuideCategories, GuideCategories),
    Guides: Object.assign(Guides, Guides),
    Inventories: Object.assign(Inventories, Inventories),
    LegalPages: Object.assign(LegalPages, LegalPages),
    PaymentGateways: Object.assign(PaymentGateways, PaymentGateways),
    PromoCodes: Object.assign(PromoCodes, PromoCodes),
    RconTasks: Object.assign(RconTasks, RconTasks),
    ServerCategories: Object.assign(ServerCategories, ServerCategories),
    Servers: Object.assign(Servers, Servers),
    ShopCategories: Object.assign(ShopCategories, ShopCategories),
    ShopCoupons: Object.assign(ShopCoupons, ShopCoupons),
    ShopItems: Object.assign(ShopItems, ShopItems),
    ShopPurchases: Object.assign(ShopPurchases, ShopPurchases),
    ShopSets: Object.assign(ShopSets, ShopSets),
    ShopStatistics: Object.assign(ShopStatistics, ShopStatistics),
    SocialLinks: Object.assign(SocialLinks, SocialLinks),
    Streams: Object.assign(Streams, Streams),
    Tickets: Object.assign(Tickets, Tickets),
    Users: Object.assign(Users, Users),
    Visits: Object.assign(Visits, Visits),
    WonItems: Object.assign(WonItems, WonItems),
}

export default Resources