import ListCases from './ListCases'
import CreateCase from './CreateCase'
import EditCase from './EditCase'

const Pages = {
    ListCases: Object.assign(ListCases, ListCases),
    CreateCase: Object.assign(CreateCase, CreateCase),
    EditCase: Object.assign(EditCase, EditCase),
}

export default Pages