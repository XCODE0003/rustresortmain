import ListBanners from './ListBanners'
import CreateBanner from './CreateBanner'
import EditBanner from './EditBanner'

const Pages = {
    ListBanners: Object.assign(ListBanners, ListBanners),
    CreateBanner: Object.assign(CreateBanner, CreateBanner),
    EditBanner: Object.assign(EditBanner, EditBanner),
}

export default Pages